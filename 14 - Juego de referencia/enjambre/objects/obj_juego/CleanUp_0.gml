// Un solo punto de limpieza (13 · 10 §12 bis.1). Este juego no crea `ds_*` ni
// superficies, así que aquí no hay nada que liberar — y se deja escrito para que el
// día que se cree la primera, se libere aquí y no en cinco sitios.
guardar_datos();
